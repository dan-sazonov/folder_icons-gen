import config

print('folder_icons readme generator v{0}.{1}.{2} by {3}'.format(config.VERSION['major'], config.VERSION['minor'],
                                                               config.VERSION['micro'], config.AUTHOR))
